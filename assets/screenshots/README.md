# Screenshots folder

Drop your screenshots here with these filenames:

1. vps-create.png     — Kamatera/Contabo VPS creation page (step 1)
2. ssh-connect.png   — Terminal showing SSH connection (step 2)  
3. install.png       — Terminal showing curl install output (step 3)
4. opencode-start.png — Terminal showing opencode command launching (step 3)
5. ctrl-p-provider.png — OpenCode Ctrl+P provider menu (step 4)
6. ctrl-p-model.png  — OpenCode Ctrl+P model picker (step 5)
7. models-list.png   — Full list of available models (step 5)
8. plan-mode.png     — Plan mode in action (modes section)
9. build-mode.png    — Build mode in action (modes section)
10. sessions.png     — Session switcher view (sessions section)

Place in: /var/www/vibecodeitbro.com/assets/screenshots/
